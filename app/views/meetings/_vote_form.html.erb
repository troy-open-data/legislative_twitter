<%= f.label :person, f.object.person.name %>
<%=
    f.collection_radio_buttons(:vote,
                               Vote.collection_text,
                               :first,
                               :last)
%>
<%= f.hidden_field :person_id %>