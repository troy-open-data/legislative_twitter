<%= simple_form_for @organization do |f| %>
  <% flash_errors_for @organization %>

  <%= f.input :level, collection: Organization::LEVELS, selected: 'Committee' %>
  <%= f.input :name %>
  <%= f.input :description %>

  <%= f.button :submit, class: 'expand' %>
<% end %>
